#pip.readthedocs.org/en/stable/installing
#download get-pip.py
#then go to the terminal and execute python3 get-pip.py 
#To download the numpy package type pip3 install numpy
# then import the package
import numpy #this is the first method of importing the package
#Numpy is an array funvtion 
numpy.array ([1,2,3,4])# An example of numpy usage
import numpy as np #this will make codes shorter by using np instead of python 
np.array ([1,2,3,4])# the code is shorter here but not so unserstandable to another person
#if you want to use only the array funvtion in numpy it is also possible 
from numpy import array
array ([1,2,3,4])
